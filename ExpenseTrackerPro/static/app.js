const $=s=>document.querySelector(s), app=$('#app'); let page='dashboard', me=null;
async function api(url,opt={}){let r=await fetch(url,{headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt}); let d=r.headers.get('content-type')?.includes('json')?await r.json():await r.text(); if(!r.ok)throw Error(d.error||'Request failed'); return d}
function landing(mode='login',msg=''){app.innerHTML=`<div class="landing"><div class="hero"><div><div class="logo">◆ Expense Tracker Pro</div><h1>Take control of your money.</h1><p>A professional personal-finance dashboard inspired by modern expense tracker interfaces: secure login, smart transactions, budgets, analytics and clean visual reports.</p></div><div class="auth-card"><div class="auth-tabs"><button class="btn ${mode==='login'?'primary':'ghost'}" onclick="landing('login')">Login</button><button class="btn ${mode==='register'?'primary':'ghost'}" onclick="landing('register')">Create Account</button></div><div id="authmsg">${msg?`<div class="error">${msg}</div>`:''}</div>${mode==='login'?`<form onsubmit="login(event)"><div class="field"><label>Email</label><input id="email" type="email" required></div><div class="field"><label>Password</label><input id="password" type="password" required></div><button class="btn primary" style="width:100%">Sign In</button></form>`:`<form onsubmit="register(event)"><div class="field"><label>Full Name</label><input id="name" required></div><div class="field"><label>Email</label><input id="email" type="email" required></div><div class="field"><label>Password</label><input id="password" type="password" minlength="6" required></div><div class="field"><label>Confirm Password</label><input id="confirm" type="password" minlength="6" required></div><button class="btn primary" style="width:100%">Create Account</button></form>`}</div></div></div>`}
async function login(e){e.preventDefault();try{await api('/api/login',{method:'POST',body:JSON.stringify({email:$('#email').value,password:$('#password').value})}); await start()}catch(x){landing('login',x.message)}}
async function register(e){e.preventDefault();if($('#password').value!==$('#confirm').value)return landing('register','Passwords do not match.');try{await api('/api/register',{method:'POST',body:JSON.stringify({name:$('#name').value,email:$('#email').value,password:$('#password').value})});await start()}catch(x){landing('register',x.message)}}
async function start(){let d=await api('/api/me');me=d.user;if(!me)return landing();shell();show(page)}
function shell(){app.innerHTML=`<div class="layout"><aside class="side"><div class="logo">◆ Expense Tracker</div><h2>Pro</h2><div class="nav"><button data-p="dashboard">▦ Dashboard</button><button data-p="transactions">⇄ Transactions</button><button data-p="analytics">◔ Analytics</button><button data-p="budget">◫ Budget</button><button onclick="location.href='/api/export'">⇩ Export CSV</button></div><div style="margin-top:auto"><div class="muted" style="color:#94a3b8;padding:10px">Signed in as<br><b style="color:white">${me.name}</b></div><button class="btn ghost" style="width:100%" onclick="logout()">Logout</button></div></aside><main class="main"><div id="content"></div></main></div>`;document.querySelectorAll('[data-p]').forEach(b=>b.onclick=()=>show(b.dataset.p))}
async function show(p){page=p;document.querySelectorAll('[data-p]').forEach(b=>b.classList.toggle('active',b.dataset.p===p));let c=$('#content');c.innerHTML='<div class="panel">Loading…</div>';try{if(p==='dashboard')await dashboard(c);if(p==='transactions')await transactions(c);if(p==='analytics')await analytics(c);if(p==='budget')await budget(c)}catch(x){c.innerHTML=`<div class="error">${x.message}</div>`}}
const money=n=>'₹'+Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:2});
async function dashboard(c){let d=await api('/api/dashboard');c.innerHTML=`<div class="top"><div><h1>Good to see you, ${me.name.split(' ')[0]} 👋</h1><span class="muted">Here’s your financial overview.</span></div><div><button class="btn primary" onclick="modal('expense')">+ Add Expense</button> <button class="btn ghost" onclick="modal('income')">+ Add Income</button></div></div><div class="cards"><div class="card"><div class="label">Total Income</div><div class="value">${money(d.summary.income)}</div></div><div class="card"><div class="label">Total Expenses</div><div class="value">${money(d.summary.expense)}</div></div><div class="card"><div class="label">Net Savings</div><div class="value">${money(d.summary.balance)}</div></div><div class="card"><div class="label">Monthly Budget</div><div class="value">${money(d.budget)}</div></div></div><div class="grid2"><div class="panel"><h3>Spending by Category</h3><div class="donut"></div><div id="catlist"></div></div><div class="panel"><h3>Recent Transactions</h3>${table(d.recent,true)}</div></div>`;$('#catlist').innerHTML=d.categories.slice(0,5).map(x=>`<div style="display:flex;justify-content:space-between;padding:8px 0"><span>${x.category}</span><b>${money(x.total)}</b></div>`).join('')||'<div class="empty">No expenses yet.</div>'}
function table(rows,small=false){if(!rows.length)return '<div class="empty">No transactions found.</div>';return `<table class="table"><thead><tr><th>Date</th><th>Type</th><th>Category</th><th>Amount</th>${small?'':'<th></th>'}</tr></thead><tbody>${rows.map(x=>`<tr><td>${x.date}</td><td><span class="pill ${x.type}">${x.type}</span></td><td>${x.category}</td><td><b>${money(x.amount)}</b></td>${small?'':`<td><button class="btn danger" onclick="delTx(${x.id})">Delete</button></td>`}</tr>`).join('')}</tbody></table>`}
async function transactions(c){c.innerHTML=`<div class="top"><div><h1>Transactions</h1><span class="muted">Search, filter and manage every entry.</span></div><button class="btn primary" onclick="modal('expense')">+ Add Transaction</button></div><div class="panel"><div class="toolbar"><input id="search" placeholder="Search transactions…" oninput="loadTx()"><select id="type" onchange="loadTx()"><option value="all">All Types</option><option value="income">Income</option><option value="expense">Expense</option></select><select id="cat" onchange="loadTx()"><option>all</option><option>Food</option><option>Transport</option><option>Shopping</option><option>Bills</option><option>Entertainment</option><option>Health</option><option>Education</option><option>Other</option></select></div><div id="tx"></div></div>`;await loadTx()}
async function loadTx(){let q=new URLSearchParams({search:$('#search')?.value||'',type:$('#type')?.value||'all',category:$('#cat')?.value||'all'});let d=await api('/api/transactions?'+q);$('#tx').innerHTML=table(d.transactions)}
async function analytics(c){let d=await api('/api/analytics');let max=Math.max(1,...d.monthly.map(x=>Math.max(x.income,x.expense)));c.innerHTML=`<div class="top"><div><h1>Analytics</h1><span class="muted">Understand your spending patterns.</span></div></div><div class="grid2"><div class="panel"><h3>Category Spending</h3><div class="donut"></div>${d.categories.map(x=>`<div style="display:flex;justify-content:space-between;padding:8px"><span>${x.category}</span><b>${money(x.total)}</b></div>`).join('')||'<div class="empty">Add expenses to see analytics.</div>'}</div><div class="panel"><h3>Monthly Income vs Expense</h3><div class="bars">${d.monthly.map(x=>`<div class="barwrap"><div class="bar" style="height:${x.income/max*170}px" title="Income ${money(x.income)}"></div><div class="bar bar2" style="height:${x.expense/max*170}px;margin-top:5px" title="Expense ${money(x.expense)}"></div><small>${x.month}</small></div>`).join('')}</div><p class="muted">Blue = income · Purple = expenses</p></div></div>`}
async function budget(c){let month=new Date().toISOString().slice(0,7),d=await api('/api/budget?month='+month);c.innerHTML=`<div class="top"><div><h1>Budget</h1><span class="muted">Set category limits and track progress.</span></div><button class="btn primary" onclick="budgetModal()">+ Set Budget</button></div><div class="panel"><table class="table"><thead><tr><th>Category</th><th>Budget</th><th>Spent</th><th>Remaining</th></tr></thead><tbody>${d.budgets.map(x=>`<tr><td>${x.category}</td><td>${money(x.amount)}</td><td>${money(x.spent)}</td><td>${money(x.remaining)}</td></tr>`).join('')||'<tr><td colspan="4" class="empty">No budgets set for this month.</td></tr>'}</tbody></table></div>`}
function modal(type){$('#app').insertAdjacentHTML('beforeend',`<div class="modal" id="modal"><div class="modal-box"><div class="top"><h2>Add ${type}</h2><button class="btn ghost" onclick="$('#modal').remove()">×</button></div><form onsubmit="saveTx(event,'${type}')"><div class="row"><div class="field"><label>Amount</label><input id="amt" type="number" min="0.01" step="0.01" required></div><div class="field"><label>Date</label><input id="dt" type="date" value="${new Date().toISOString().slice(0,10)}" required></div></div><div class="field"><label>${type==='income'?'Source':'Category'}</label><input id="catx" list="cats" required><datalist id="cats"><option>Food</option><option>Transport</option><option>Shopping</option><option>Bills</option><option>Entertainment</option><option>Health</option><option>Education</option><option>Salary</option><option>Freelance</option><option>Other</option></datalist></div><div class="field"><label>Description</label><input id="desc"></div><div class="field"><label>Payment Method</label><select id="pay"><option>UPI</option><option>Cash</option><option>Card</option><option>Bank Transfer</option><option>Other</option></select></div><button class="btn primary" style="width:100%">Save ${type}</button></form></div></div>`)}
async function saveTx(e,type){e.preventDefault();await api('/api/transactions',{method:'POST',body:JSON.stringify({type,amount:$('#amt').value,category:type==='expense'?$('#catx').value:'Income',source:type==='income'?$('#catx').value:'',description:$('#desc').value,date:$('#dt').value,payment_method:$('#pay').value})});$('#modal').remove();show(page)}
async function delTx(id){if(confirm('Delete this transaction?')){await api('/api/transactions/'+id,{method:'POST',body:'{}'});loadTx()}}
function budgetModal(){app.insertAdjacentHTML('beforeend',`<div class="modal" id="modal"><div class="modal-box"><h2>Set Monthly Budget</h2><form onsubmit="saveBudget(event)"><div class="field"><label>Category</label><input id="bc" required></div><div class="field"><label>Amount</label><input id="ba" type="number" min="0" step="0.01" required></div><button class="btn primary">Save Budget</button> <button type="button" class="btn ghost" onclick="$('#modal').remove()">Cancel</button></form></div></div>`)}
async function saveBudget(e){e.preventDefault();await api('/api/budget',{method:'POST',body:JSON.stringify({month:new Date().toISOString().slice(0,7),category:$('#bc').value,amount:$('#ba').value})});$('#modal').remove();show('budget')}
async function logout(){await api('/api/logout',{method:'POST'});me=null;landing()}
start().catch(()=>landing());
